h1(style="color: blue") User Management (CRUD)
h2(style="color: green") User list
table(style="width: 400px; text-align: left;")
  tr
    th User ID
    th Name
    th Change username
    th Delete
  each user in users  
    tr
      td= user.id
      td= user.name
      td
        a(href="/users/" + user.id + "/changename") Click
      td
        a(href="/users/" + user.id + "/delete") Click
        
h2(style="color: orange") Add new user
form(action="/users", method="post")
  label Enter name:
  br
  input(type="text", name="name")
  br
  button Add

  